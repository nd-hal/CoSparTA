% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcpp_wrappers.R
\name{calc_EZ_3d_fast}
\alias{calc_EZ_3d_fast}
\title{Fast Sparse EZ Aggregation Wrapper}
\usage{
calc_EZ_3d_fast(x, alpha_k, n, p, w)
}
\arguments{
\item{x}{A data.table with integer columns \code{V1}, \code{V2}, \code{V3}
(1-based indices) and a numeric column \code{x} (observed counts).}

\item{alpha_k}{Numeric vector of length \code{nrow(x)}: rank-k
responsibilities.}

\item{n}{Integer. L-mode (user) dimension.}

\item{p}{Integer. F-mode (time) dimension.}

\item{w}{Integer. W-mode (channel) dimension.}
}
\value{
Named list with \code{rs} (length n), \code{cs} (length p),
  \code{zs} (length w).
}
\description{
Thin R wrapper around \code{\link{calc_EZ_3d_cpp}} that accepts the same
sparse coordinate-format data.table \code{x} used by \code{calc_EZ_3d()},
making the Rcpp replacement a drop-in at the call site in
\code{CoSparTA()} and \code{CoSparTA_missing()}.
}
\keyword{internal}
