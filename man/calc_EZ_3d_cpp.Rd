% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{calc_EZ_3d_cpp}
\alias{calc_EZ_3d_cpp}
\title{Sparse Weighted Aggregation for Tensor EM}
\usage{
calc_EZ_3d_cpp(V1, V2, V3, x_vals, alpha_k, n, p, w)
}
\arguments{
\item{V1}{Integer vector of user (L-mode) indices (1-based).}

\item{V2}{Integer vector of time (F-mode) indices (1-based).}

\item{V3}{Integer vector of channel (W-mode) indices (1-based).}

\item{x_vals}{Numeric vector of observed counts at each nonzero position.}

\item{alpha_k}{Numeric vector of length \code{nnz}: the rank-k responsibility
weights (alpha[, k]).}

\item{n}{Integer. Number of users (L-mode dimension).}

\item{p}{Integer. Number of time points (F-mode dimension).}

\item{w}{Integer. Number of channels (W-mode dimension).}
}
\value{
A named list with three numeric vectors:
  \item{rs}{Length-\code{n} vector: row sums (L-mode aggregation).}
  \item{cs}{Length-\code{p} vector: column sums (F-mode aggregation).}
  \item{zs}{Length-\code{w} vector: slice sums (W-mode aggregation).}
}
\description{
For a sparse 3-way tensor stored in coordinate format, computes the
rank-k sufficient statistics (row sums, column sums, slice sums) needed
for the EM update of factor matrices L, F, W. Replaces the dplyr
group-by approach in \code{calc_EZ_3d()}.
}
