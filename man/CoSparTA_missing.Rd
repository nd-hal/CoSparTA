% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{CoSparTA_missing}
\alias{CoSparTA_missing}
\title{Covariate-aware Empirical Bayes Tensor Decomposition with Missing Data}
\usage{
CoSparTA_missing(
  X,
  K,
  Xcov = NULL,
  obs_mask = NULL,
  lib_size = NULL,
  init = "random_gamma",
  maxiter = 100,
  maxiter_init = 100,
  tol = 1e-06,
  ebpm.fn = c(ebpm_point_gamma_multiplier_covariates, ebps_with_uq,
    ebpm_point_gamma_with_uq),
  fix_L = FALSE,
  fix_F = FALSE,
  fix_W = FALSE,
  smooth_F = TRUE,
  printevery = 10,
  verbose = TRUE,
  adj_LF_scale = TRUE,
  convergence_criteria = "ELBO",
  n_stable = 3L,
  U1_true = NULL,
  U2_true = NULL,
  U3_true = NULL
)
}
\arguments{
\item{X}{A 3-dimensional non-negative integer array of dimensions
\code{n x p x w}. May contain \code{NA} for missing entries.}

\item{K}{Integer. Number of components (CP rank).}

\item{Xcov}{Covariate input for the observation mode. Can be: (1) \code{NULL}
for fully unsupervised decomposition; (2) a numeric matrix of dimension
\code{n x q}, applied identically to all K components; or (3) a list of
length \code{K}, where each element is either a numeric covariate matrix
(dimensions \code{n x q_k}, potentially different numbers of covariates per
component) or \code{NULL} for unsupervised components. Default \code{NULL}.}

\item{obs_mask}{Optional logical array of same dimensions as \code{X},
where \code{TRUE} indicates observed entries. If \code{NULL}, inferred
from \code{is.na(X)}. Default \code{NULL}.}

\item{lib_size}{Numeric vector of length \code{n}. Default \code{NULL}.}

\item{init}{Initialization method. Default \code{'random_gamma'}.}

\item{maxiter}{Maximum EM iterations. Default \code{100}.}

\item{maxiter_init}{Maximum initialization iterations. Default \code{100}.}

\item{tol}{Convergence tolerance. Default \code{1e-6}.}

\item{ebpm.fn}{List of three EBPM functions for L, F, W modes respectively.
Default uses \code{ebpm_point_gamma_multiplier_covariates} for L,
\code{ebps_with_uq} for F, and \code{ebpm_point_gamma_with_uq} for W.}

\item{fix_L}{Logical. Fix L at initialization. Default \code{FALSE}.}

\item{fix_F}{Logical. Fix F at initialization. Default \code{FALSE}.}

\item{fix_W}{Logical. Fix W at initialization. Default \code{FALSE}.}

\item{smooth_F}{Logical. Apply smoothing to F. Default \code{TRUE}.}

\item{printevery}{Print progress every this many iterations. Default
\code{10}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}

\item{adj_LF_scale}{Logical. Rescale L and F each iteration. Default
\code{TRUE}.}

\item{convergence_criteria}{Convergence criterion: \code{'ELBO'} (default)
or \code{'mKLabs'}.}

\item{n_stable}{Integer. Number of consecutive iterations with factor change
below \code{tol} required before declaring convergence. Only used when
\code{convergence_criteria = 'factor_change'}. Default \code{3}.}

\item{U1_true}{Optional true L matrix for simulation evaluation.}

\item{U2_true}{Optional true F matrix for simulation evaluation.}

\item{U3_true}{Optional true W matrix for simulation evaluation.}
}
\value{
Same structure as \code{\link{CoSparTA}} (including all
\code{_normed} fields) with one additional field:
\describe{
  \item{obs_structure}{Internal observation structure used during fitting,
    required by \code{\link{evaluate_missing_prediction}}.}
}
}
\description{
Extends \code{\link{CoSparTA}} to handle tensors with missing entries.
Missing entries are excluded from the likelihood and the ELBO, and
per-observation scales are computed using only observed entries for each
mode. The generative model and priors are identical to
\code{\link{CoSparTA}}.
}
\examples{
\dontrun{
set.seed(42)
X <- array(rpois(20 * 12 * 4, lambda = 1.5), dim = c(20, 12, 4))
mask_info <- generate_missing_mask(X, missing_rate = 0.1, seed = 42)

# With covariates
Xcov <- matrix(rnorm(20 * 2), nrow = 20)
fit <- CoSparTA_missing(mask_info$X_obs, K = 3, Xcov = Xcov,
                        obs_mask = mask_info$obs_mask)

# Without covariates
fit0 <- CoSparTA_missing(mask_info$X_obs, K = 3,
                         obs_mask = mask_info$obs_mask)
}

}
\seealso{
\code{\link{CoSparTA}}, \code{\link{generate_missing_mask}},
  \code{\link{evaluate_missing_prediction}}
}
